{
  "name": "@seo-autopilot/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@seo-autopilot/connectors": "workspace:*",
    "@seo-autopilot/core": "workspace:*",
    "@seo-autopilot/database": "workspace:*",
    "@seo-autopilot/site-adapters": "workspace:*",
    "zod": "^3.24.1"
  }
}
